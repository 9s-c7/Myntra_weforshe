import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginBottom:5,
  },
  scrollContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 10,
  },
  imageContainer: {
    marginRight: 10,
  },
  image: {
    width: 80,
    height: 110,
  },
});
